// This file is separate from constants because it has more to do with
// UI than business logic. `constants.ts` may be refactored later.
import Favicon from '../../public/favicon2x.png'

export default Favicon