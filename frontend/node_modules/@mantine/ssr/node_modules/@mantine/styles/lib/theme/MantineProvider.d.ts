import React from 'react';
import type { Options as EmotionCacheOptions } from '@emotion/cache';
import type { MantineThemeOverride, MantineTheme } from './types';
import type { CSSObject } from '../tss';
declare type ProviderStyles = Record<string, Record<string, CSSObject> | ((theme: MantineTheme, params: Record<string, any>) => Record<string, CSSObject>)>;
declare type ProviderClassNames = Record<string, Record<string, string>>;
declare type MantineDefaultProps = Record<string, Record<string, any>>;
export declare function useMantineTheme(): MantineTheme;
export declare function useMantineThemeStyles(component: string): {
    styles: Record<string, CSSObject> | ((theme: MantineTheme, params: Record<string, any>) => Record<string, CSSObject>);
    classNames: Record<string, string>;
};
export declare function useMantineEmotionOptions(): EmotionCacheOptions;
export declare function useMantineDefaultProps<T extends Record<string, any>>(component: string, defaultProps: Partial<T>, props: T): T;
export interface MantineProviderProps {
    theme?: MantineThemeOverride;
    styles?: ProviderStyles;
    classNames?: ProviderClassNames;
    defaultProps?: MantineDefaultProps;
    emotionOptions?: EmotionCacheOptions;
    withNormalizeCSS?: boolean;
    withGlobalStyles?: boolean;
    withCSSVariables?: boolean;
    children: React.ReactNode;
    inherit?: boolean;
}
export declare function MantineProvider({ theme, styles, classNames, defaultProps, emotionOptions, withNormalizeCSS, withGlobalStyles, withCSSVariables, inherit, children, }: MantineProviderProps): JSX.Element;
export declare namespace MantineProvider {
    var displayName: string;
}
export {};
//# sourceMappingURL=MantineProvider.d.ts.map